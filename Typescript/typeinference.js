var empName;
empName = 'ajay';
var empnamelist = ['ajay', 'kavya', 'akshana', 'aksharan'];
function add(a, b) {
    return a + b;
}
//union and intersection
//union type inference (OR Condition - pass multiple value)
var browser;
browser = 'Chrome';
browser = null;
var responseStatus;
responseStatus = 'created';
responseStatus = 201;
//intersections 
